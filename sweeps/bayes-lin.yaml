command:
  - python
  - train.py
  - --dataset=mnist
  - --is_linear
  - --x_shape=28
  - --y_shape=28
  - ${args}
method: bayes
metric:
  goal: minimize
  name: eval_loss
parameters:
  learning_rate:
    max: 0.0003
    min: 0.000001
    distribution: uniform